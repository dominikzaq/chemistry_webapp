{% extends 'base.html.twig' %}

{% block title %}Fik ref list{% endblock %}

{% block body %}
    <h1>List aki ref</h1>
    <table class="table">
        <thead>
        <tr>
            <th scope="col">id</th>
            <th scope="col">ref</th>
            <th scope="col">reference</th>
            <th scope="col">edit</th>
            <th scope="col">delete</th>
        </tr>
        </thead>
        <tbody>
        {% for ar in aki_ref %}
            <tr>
                <td>
                    <a href="{{ path('jonization_level_edit', {'id': ar.id}) }}">Edit</a>
                </td>
                <td>
                    <a href="{{ path('energy_level_list', {'id': ar.id }) }}">List</a>
                </td>
                <td>
                    <a href="{{ path('energy_level_add', {'id': ar.id }) }}">Add</a>
                </td>
                <td>
                    <a href="{{ path('jonization_level_delete', {'id': ar.id}) }}">Delete</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
