{% extends 'base.html.twig' %}

{% block title %}Fik ref view{% endblock %}

{% block body %}
    <h1>View aki ref</h1>
    <table class="table">
        <thead>
        <tr>
            <th scope="col">id</th>
            <th scope="col">ref</th>
            <th scope="col">reference</th>
        </tr>
        </thead>
        <tbody>
        {% for ar in aki_ref %}
            <tr>
                <td text="editable">{{ ar.name }}</td>
                <td>
                    <a href="{{ path('jonization_level_edit', {'id': ar.id}) }}">Edit</a>
                </td>
                <td>
                    <a href="{{ path('energy_level_list', {'id': ar.id }) }}">List</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
